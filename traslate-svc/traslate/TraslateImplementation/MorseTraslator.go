package TraslateImplementation

/*import (
	"errors"
	morseTras "github.com/morse-master"
	"strings"

)


func(t *translator) StringToMorse(text string)(string, error){
	h := morseTras.NewHacker()
	morseCode, err := h.Encode(strings.NewReader(text))
	if err != nil {
		return "", errors.New("cannot Convert this to Morse")
	}
	return string(morseCode), nil
}*/
